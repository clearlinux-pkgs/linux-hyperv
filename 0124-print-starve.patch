From 62ace7230dee848a2582c1dc4f141315322b16c5 Mon Sep 17 00:00:00 2001
From: Arjan van de Ven <arjan@linux.intel.com>
Date: Fri, 29 Jul 2016 19:20:26 +0000
Subject: [PATCH 124/131] print starve

Clearly print when we're starved for entropy
---
 drivers/char/random.c | 2 ++
 1 file changed, 2 insertions(+)

diff --git a/drivers/char/random.c b/drivers/char/random.c
index 38e0ed399a5e..ddc1634d0404 100644
--- a/drivers/char/random.c
+++ b/drivers/char/random.c
@@ -1867,9 +1867,11 @@ _random_read(int nonblock, char __user *buf, size_t nbytes)
 		if (nonblock)
 			return -EAGAIN;
 
+		printk("clr: Blocking on entropy: %s\n", current->comm);
 		wait_event_interruptible(random_read_wait,
 			ENTROPY_BITS(&input_pool) >=
 			random_read_wakeup_bits);
+		printk("clr: Done Blocking on entropy: %s\n", current->comm);
 		if (signal_pending(current))
 			return -ERESTARTSYS;
 	}
-- 
2.20.1

